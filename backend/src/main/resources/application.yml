spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/app
    username: user
    password: pass

  jpa:
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: "*"